export default class Order {
    constructor(id, orderItems, total, orderDate){
        this.id = id;
        this.orderItems = orderItems,
        this.total = total,
        this.orderDate = orderDate
    }
}